<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="horizontal"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="20sp"
        android:text="@string/count_price"
        android:layout_centerVertical="true"
        android:layout_toLeftOf="@+id/count_price"
        android:layout_toStartOf="@+id/count_price" />
    <TextView
        android:id="@+id/count_price"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="20sp"
        android:textColor="@android:color/holo_red_light"
        android:text="¥8.80"
        android:layout_marginRight="89dp"
        android:layout_marginEnd="89dp"
        android:layout_centerVertical="true"
        android:layout_toLeftOf="@+id/btn_submit_order"
        android:layout_toStartOf="@+id/btn_submit_order" />
    <Button
        android:id="@+id/btn_submit_order"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_marginRight="10dp"
        android:textSize="18sp"
        android:text="@string/submit_order"/>
</RelativeLayout>